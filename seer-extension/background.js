chrome.runtime.onInstalled.addListener(()=>chrome.storage.local.set({enabled:true,apiBase:'http://127.0.0.1:8080'}));
chrome.runtime.onMessage.addListener((message,sender,sendResponse)=>{if(message.type==='scan'){chrome.storage.local.get({apiBase:'http://127.0.0.1:8080'},async c=>{try{const r=await fetch(c.apiBase+'/api/v1/scan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:message.text,sourceDomain:new URL(sender.tab.url).hostname,inputMode:'PASTE',policyProfile:'PERSONAL'})});sendResponse(await r.json())}catch(e){sendResponse({error:'Local SEER service unavailable'})}});return true}});
