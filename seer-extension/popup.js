const enabled=document.querySelector('#enabled');chrome.storage.local.get({enabled:true},x=>enabled.checked=x.enabled);enabled.onchange=()=>chrome.storage.local.set({enabled:enabled.checked});fetch('http://127.0.0.1:8080/api/v1/health').then(r=>r.ok?document.querySelector('#status').textContent='Connected to local SEER':Promise.reject()).catch(()=>document.querySelector('#status').textContent='Start SEER on localhost:8080');
